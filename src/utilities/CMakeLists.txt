cmake_minimum_required (VERSION 2.8)

set (UTILITIES_SOURCE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/src/utilities")

set (UTILITIES_HEADER_FILES
    ${UTILITIES_SOURCE_DIR}/Error.h
    ${UTILITIES_SOURCE_DIR}/Lock.h
)

set (UTILITIES_SOURCE_FILES
    ${UTILITIES_SOURCE_DIR}/Error.cpp
)

list (APPEND HEADER_FILES ${UTILITIES_HEADER_FILES})
list (APPEND SOURCE_FILES ${UTILITIES_SOURCE_FILES})

source_group (utilities FILES ${UTILITIES_HEADER_FILES} ${UTILITIES_SOURCE_FILES})

include_directories (${UTILITIES_SOURCE_DIR})